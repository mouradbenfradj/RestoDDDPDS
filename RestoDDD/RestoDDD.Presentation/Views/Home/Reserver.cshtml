@model RestoDDD.Application.Entities.Reservation_DTO

@{
    ViewBag.Title = "Reserver";
}

<h2>Reserver</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Reservation_DTO</h4>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.LabelFor(model => model.NombrePres, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.NombrePres)
                @Html.ValidationMessageFor(model => model.NombrePres)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.ResvDate, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.ResvDate)
                @Html.ValidationMessageFor(model => model.ResvDate)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
